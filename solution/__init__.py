"""Project code for coordinate mapping pipeline.

The `solution` package hosts reusable logic for dataset IO/validation, training,
prediction, and evaluation, used by thin CLI entrypoints (e.g. `train.py`,
`predict.py`, `evaluate.py`, `run_pipeline.py`).
"""